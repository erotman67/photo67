# Files that live in the repo but must NOT be served as part of the website.
# Wrangler uploads everything in the root by default — including .git — so
# anything listed here is deliberately kept off the public site.
.git
.github
scripts
node_modules
wrangler.json
.assetsignore
.gitignore
README.md
HOW-TO-ADD-PHOTOS.md
